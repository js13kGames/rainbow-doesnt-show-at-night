export type Player = { speed: number }
export type Wobble = { prevX: number; prevY: number }
export type Squash = { t: number; base: number }
export type Jump = { dx: number; dy: number; t: number }
export type Cloud = { speed: number }
