// Below data describes the alphabet pixels position from the 'sprite-sheet.png'.
//
// 'A' starts at (16, 48) pixel from sprite-sheet.png(top-left corner is (0, 0)).
const START_X = 16;
const START_Y = 48;

// Each alphabets have 3 x 5 size each.
const CHAR_WIDTH = 3;
const CHAR_HEIGHT = 5;

// Sprite sheet consist of 12 columns, making A to L fits in first row.
// M to X is in 2nd row, and Y, Z is in 3rd row.
const COLUMNS = 12;
const ROWS = 3;
